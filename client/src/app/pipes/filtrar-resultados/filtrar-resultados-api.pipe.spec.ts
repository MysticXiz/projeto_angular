import { FiltrarResultadosApiPipe } from './filtrar-resultados-api.pipe';

describe('FiltrarResultadosApiPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarResultadosApiPipe();
    expect(pipe).toBeTruthy();
  });
});
